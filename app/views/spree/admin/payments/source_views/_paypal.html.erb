<fieldset data-hook="paypal">
  <legend align="center"><%= t(:transaction, :scope => :paypal) %></legend>

  <div class="row">
    <div class="alpha six columns">
      <dl>
        <dt><%= t(:payer_id, :scope => :paypal) %>:</dt>
        <dd><%= payment.source.payer_id %></dd>

        <dt><%= t(:token, :scope => :paypal) %>:</dt>
        <dd><%= payment.source.token %></dd>

        <dt><%= t(:transaction_id) %>:</dt>
        <dd><%= payment.source.transaction_id %></dd>
      </dl>
    </div>

    <% if payment.source.state != 'refunded' %>
      <%= button_link_to t('actions.refund', :scope => :paypal), spree.paypal_refund_admin_order_payment_path(@order, payment), :icon => 'icon-dollar' %>
    <% else %>
      <div class="alpha six columns">
        <dl>
          <dt><%= t(:state, :scope => :paypal) %>:</dt>
          <dd><%= payment.source.state.titleize %></dd>

          <dt><%= t(:refunded_at, :scope => :paypal) %>:</dt>
          <dd><%= pretty_time(payment.source.refunded_at) %></dd>

          <dt><%= t(:refund_transaction_id, :scope => :paypal) %>:</dt>
          <dd><%= payment.source.refund_transaction_id %></dd>
        </dl>
      </div>
    <% end %>
  </div>
</fieldset>